# Monorepo: Railway service "Root Directory" MUST be `backend`.
# Uses Dockerfile so Railpack/Nixpacks auto-detect issues are avoided.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
